import app from './app.js';import { env } from './config/env.js';import { pool } from './config/database.js';
try{await pool.query('SELECT 1')}catch(error){if(env.NODE_ENV==='production'){console.error('Unable to connect to MySQL. Check backend/.env and database/schema.sql.');console.error(error.message);process.exit(1)}app.locals.demoMode=true;console.warn('MySQL is unavailable; running the clearly labelled in-memory demo API. Data will not persist.')}
const host=app.locals.demoMode?'127.0.0.1':'0.0.0.0';const server=app.listen(env.PORT,host,()=>console.log(`Archirani API listening at http://${host}:${env.PORT}${app.locals.demoMode?' (demo mode)':''}`));const shutdown=async()=>{server.close(async()=>{await pool.end();process.exit(0)})};process.on('SIGTERM',shutdown);process.on('SIGINT',shutdown);
